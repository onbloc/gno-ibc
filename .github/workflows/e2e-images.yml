name: Build E2E images

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.gno-version'
      - '.dockerignore'
      - '.gitmodules'
      - 'Makefile'
      - 'e2e/images/Dockerfile'
      - '.github/scripts/ensure-image.sh'
      - '.github/scripts/image-tag.sh'
      - 'e2e/images/union-deployer/default.nix'
      - 'e2e/images/union-deployer/trusted-mpt.patch'
      - 'e2e/gno/entrypoint.sh'
      - 'e2e/gno/setup.sh'
      - 'e2e/config/config.jsonc.template'
      - 'e2e/config/devnet.json'
      - 'gno.land/**'
      - 'third_party/**'
      - '.github/workflows/e2e-images.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        component: [gno, voyager, union-deployer]
    steps:
      - name: Check out gno-ibc
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: ${{ matrix.component == 'gno' && 'recursive' || 'false' }}

      - name: Load disposable devnet configuration
        id: devnet
        run: python3 e2e/provision.py export-env >>"$GITHUB_OUTPUT"

      - name: Check out pinned union-voyager
        if: matrix.component == 'voyager' || matrix.component == 'union-deployer'
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: onbloc/union-voyager
          ref: ${{ steps.devnet.outputs.UNION_VOYAGER_REVISION }}
          path: union-voyager

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Resolve or build image
        env:
          E2E_REGISTRY: ghcr.io
          E2E_IMAGE_NAMESPACE: ${{ github.repository_owner }}
          E2E_GNO_REPOSITORY: ${{ vars.E2E_GNO_REPOSITORY }}
          E2E_VOYAGER_REPOSITORY: ${{ vars.E2E_VOYAGER_REPOSITORY }}
          E2E_UNION_DEPLOYER_REPOSITORY: ${{ vars.E2E_UNION_DEPLOYER_REPOSITORY }}
          UNION_VOYAGER_DIR: ${{ github.workspace }}/union-voyager
          UNION_VOYAGER_REVISION: ${{ steps.devnet.outputs.UNION_VOYAGER_REVISION }}
        run: |
          image=$(.github/scripts/ensure-image.sh "${{ matrix.component }}" --push)
          printf '### %s\n\n%s\n' "${{ matrix.component }}" "$image" >>"$GITHUB_STEP_SUMMARY"
